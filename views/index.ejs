<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <span>Welcome to the Home Page!</span>
    <% messages.forEach(message => { %>
    <div class="message">
      <h2><%= message.title %></h2>
      <p>
        <small>Posted by <%= message.user %> on <%= message.added %></small>
      </p>
    </div>
    <% }); %>
  </body>
</html>
